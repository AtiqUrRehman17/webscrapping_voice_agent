[project]
name = "web-voice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv>=1.2.2",
]

[tool.uv.workspace]
members = [
    "livekit-web-agent",
]
